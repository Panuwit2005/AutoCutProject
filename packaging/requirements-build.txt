# Build-time dependencies for packaging AutoCut Pro into a Windows app.
# Install into the build venv (Python 3.11):
#   .buildvenv\Scripts\python.exe -m pip install -r requirements-build.txt

# Runtime app deps (pinned to stable versions known to ship clean wheels on 3.11)
flask==3.0.3
flask-cors==4.0.1
faster-whisper==1.1.0
requests==2.32.3
waitress==3.0.0

# Desktop app shell (native window) + offline license signing
pywebview==5.3.2
pythonnet==3.0.5
cryptography==44.0.0

# Packaging
pyinstaller==6.11.1
